Link        :initialize(params)         # => link
            :add_tag(tag)               # => tag symbol
            :url                        # => URL string
            :title                      # => title string


List        :initialize                 # => list
            :link_new(link)             # => link
            :delete_link(link)          # => link
            :links                      # => array of links
            :filter(tag)                # => array of links filtered by tag
