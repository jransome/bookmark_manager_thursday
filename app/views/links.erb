<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Links</title>
  </head>
  <body>
    <h1>Links</h1>
    <ul id='links'>
      <% @links.each do |link| %>
      <li>
        Title:  <%= link.title %>
        URL:    <%= link.url %>
        Tag:    <%= link.tags.last.name if link.tags.last %>
      </li>
      <% end %>
    </ul>
  </body>
</html>
